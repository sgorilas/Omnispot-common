# Folder to place downloaded deployment descriptors 
deployment_path=#{bootstrap.home}/files/deployment
# Folder to place downloaded resources
resource_path=#{bootstrap.home}/files/resource
db_host=localhost
db_port=1527
db_location=#{bootstrap.home}/data/client
# Bootstrap Derby DB URL
jdbc_url=jdbc:derby://localhost:1527/#{bootstrap.home}/data/client;create=false;user=kesdipepe;password=kesdipepe
driver_class=#{jdbc.driver.class}
# Interval in msec between successive updates of Action statuses in the DB
timing_handler_sleep_period=60000
# Retries to download a resource
resource_retrylimit=3
# Classpath to launch the player process
player_classpath=#{bootstrap.home}/conf;#{bootstrap.home}/lib/player.jar;#{bootstrap.home}/lib/common.jar;#{bootstrap.home}/lib/bootstrap.jar;#{bootstrap.home}/lib/spring.jar;#{bootstrap.home}/lib/log4j-1.2.14.jar;#{bootstrap.home}/lib/derbyclient.jar;#{bootstrap.home}/lib/commons-pool-1.4.jar;#{bootstrap.home}/lib/jvlc-core-0.9.0-SNAPSHOT-20080727.jar;#{bootstrap.home}/lib/commons-logging-1.0.4.jar;#{bootstrap.home}/lib/commons-dbcp-1.2.2.jar;#{bootstrap.home}/lib/quartz-1.6.3.jar;#{bootstrap.home}/lib/examples.jar;#{bootstrap.home}/lib/rome-1.0RC1.jar;#{bootstrap.home}/lib/jdom.jar;#{bootstrap.home}/lib/comfyj-2.7.jar;#{bootstrap.home}/lib/jniwrap-3.8.jar;#{bootstrap.home}/lib/winpack-3.8.jar;#{bootstrap.home}/lib/commons-lang.jar;#{bootstrap.home}/lib/jaxen-1.1.1.jar;#{bootstrap.home}/lib/dom4j-1.6.1.jar;#{bootstrap.home}/lib/java-engine.jar;#{bootstrap.home}/lib/commons-collections-3.1.jar

player_main_class=com.kesdip.player.Player
# Working directory for the player process
player_working_dir=#{bootstrap.home}/player
# Interval between consecutive player communications to server
communication_interval=20
# The command servlet's URL 
server_url=http://#{admin.server.host}:#{admin.server.port}/admin-console/servlet/commandServlet
# The interval between consecutive screen dumps
screendump_interval=40
# Dimensions of screen dump (width*height)
# A non-numeric value indicates a calculated value (i.e. maintain proportions)
# If both values are non-numeric, the image is not resized
screendump_size=x*480
# The folder where the screen dump file will be stored
screendump_storage_path=#{bootstrap.home}/files/print-screens
# The unique id of the current installation 
installation_id=ADFG32666VCXZHVS
